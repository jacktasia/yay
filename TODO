TODO

-- keep only one image image intemp folder based on size of tray and then add..

-- remove created cachedir on start?

-- repo for good wallpapers

-- pruning issue, doesn't happen until .next()

-- support for: vista/7,KDE, and maybe mac.

-- "smart" resize -- Image.getScaledInstance() see rez of computer and change file rez to equal or below but keep ratio

-- Ability to specify pattern style (stretch, tile, center, etc)

-- Ability to pull in images from nested folders.

-- handle if saved pictures directory is not found on start up...Change Dir / Quit ...dialog reloadTime

-- prune feature - vote up and down...

-- have rename file feature...click button

-- have option to start slideshow on start up

-- feature to view all images on maxed screen ...maybe 4 to a screen with their and voting....BIG VIEW
